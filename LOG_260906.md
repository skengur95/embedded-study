# 임베디드 / 온디바이스 AI 독학 로그

> HYUNDAI AI Insight Campus 온디바이스 AI 과정(2026.08.24 ~ 2027.04.05)과 병행하는 개인 학습 기록.
> 수업이 못 주는 **MCU 베어메탈 감각**을 채우는 것이 목적.

---

## 최종 목표

- **주무기**: 임베디드 C / 제어
- **얹을 것**: AI 모델 경량화 · 온디바이스 배포
- **결과물**: MCU에서 진동 데이터로 이상을 감지하는 시스템 (예지보전)
- **타겟**: 현대무벡스(물류자동화 R&D, 제어개발) · 현대엘리베이터(IoT·예지보전)

## 공고 검색 키워드

임베디드 SW 개발자 / 임베디드 AI / Edge AI 엔지니어 / AI 모델 경량화 · 최적화 / 제어 SW · 펌웨어 개발 / 비전 AI / 로보틱스 SW

---

## 전체 로드맵

| 월 | 목표 |
|---|---|
| 9월 | 베어메탈 — LED, 버튼, UART (레지스터 직접 제어) |
| 10월 | 인터럽트 + IMU 센서 100Hz 수집, 스텝모터 |
| 11월 | FreeRTOS, 로직 애널라이저로 지터 측정 |
| 12월 | 미니 엘리베이터 + 정상/이상 데이터 수집, PC에서 FFT + 모델 학습 |
| 1월 | **TFLite Micro로 MCU에 모델 탑재** (핵심) |
| 2월 | 양자화·최적화, Jetson과 비교 |
| 3월 | 문서화, 포트폴리오 정리 |

**시간 배분**: 평일 저녁 40분 + 주말 3시간 = 주 7시간

---

## 개발 환경 (2026.09 기준)

| 항목 | 버전 / 비고 |
|---|---|
| 보드 | STM32 Nucleo-F411RE (Cortex-M4, Flash 512KB, RAM 128KB) |
| MCU | STM32F411RETx |
| IDE | STM32CubeIDE 2.2.0 |
| 코드 생성 | **STM32CubeMX 6.18.1 (별도 설치 필수)** |
| 펌웨어 팩 | STM32Cube FW_F4 V1.28.3 |
| 워크스페이스 | `E:\embedded-study\workspace` |

### ⚠️ CubeIDE 2.0부터 CubeMX가 분리됨

1.x에서는 CubeIDE 안에 `File > New > STM32 Project`가 있었으나 **2.0.0부터 CubeMX 플러그인이 제거됨.**
CubeIDE 단독으로는 보드 선택·HAL 포함 프로젝트를 만들 수 없다.

**올바른 순서**
1. CubeMX 실행 → ACCESS TO BOARD SELECTOR
2. NUCLEO-F411RE → Start Project → "Initialize all peripherals?" **Yes**
3. Project Manager 탭 → 이름/경로 지정, Toolchain을 **STM32CubeIDE**로
4. GENERATE CODE → Open Project

### 보드 핀 메모

- 사용자 LED = **PA5** (LD2, 초록, 칩 21번 핀)
- 사용자 버튼 = **PC13** (B1, 칩 2번 핀)
- UART(가상 시리얼) = **PA2 / PA3**, 보드레이트 115200

### 문서 (ST 공식, 무료)

- **RM0383** 레퍼런스 매뉴얼 — 레지스터 사전. 제일 많이 봄
  `st.com/resource/en/reference_manual/rm0383-stm32f411xce-advanced-armbased-32bit-mcus-stmicroelectronics.pdf`
- **DS10314** 데이터시트 — 전기 스펙
  `st.com/resource/en/datasheet/stm32f411re.pdf`
- 읽는 법: 처음부터 읽지 말고 Ctrl+F로 찾아 쓰기

---

## 레지스터 노트

### 오늘 쓴 3개

| 레지스터 | 하는 일 | 근거 |
|---|---|---|
| `RCC->AHB1ENR` | 주변장치 클럭 공급 (0=GPIOA, 1=B, 2=C) | RM0383 6.3.9 |
| `GPIOx->MODER` | 핀 모드 (핀당 **2비트**) | RM0383 8.4.1 |
| `GPIOx->ODR` | 출력값 (핀당 1비트) | RM0383 8.4.6 |

### MODER 계산법

핀 y는 **비트 2y ~ 2y+1** 사용.
- PA5 → 비트 10~11 → 출력(`01`) → `MODER |= (1 << 10)`
- PC13 → 비트 26~27 → 입력(`00`, 리셋 기본값이라 생략 가능)
  - 단, 실무에선 명시적으로: `GPIOC->MODER &= ~(3 << 26);`

MODER 값: `00` 입력 / `01` 출력 / `10` 대체기능(I2C·UART·PWM) / `11` 아날로그

### 비트 마스킹 3종

| 목적 | 연산 |
|---|---|
| 켜기 | `\|= (1 << n)` |
| 끄기 | `&= ~(1 << n)` |
| 뒤집기 | `^= (1 << n)` |

통째로 `=`로 덮으면 같은 레지스터의 다른 핀 설정이 날아감.

---

## 9월 주차별 계획

### 준비 ✅
- [x] Nucleo-F411RE 주문 · 수령
- [x] STM32CubeIDE 2.2.0 + CubeMX 6.18.1 설치
- [x] ST-Link 펌웨어 업데이트
- [x] 추가 부품 주문 (화요일 9/8 도착 예정)

### 1주차 · 일단 불 켜기 ✅
- [x] HAL로 LED 깜빡이기 (500ms)
- [x] 깜빡임 속도 변경

### 2주차 · 레지스터로 직접 ✅ (조기 완료)
- [x] RCC로 GPIOA 클럭 공급
- [x] MODER로 PA5 출력 모드 설정
- [x] ODR로 LED 제어
- [x] RM0383에서 비트 위치 직접 확인
- [x] `HAL_Delay` → 지연 루프로 교체
- [ ] 깃허브 저장소 생성 + 첫 커밋

### 3주차 · 버튼
- [ ] PC13 버튼 읽기 (**GPIOC 클럭 별도로 켜야 함**)
- [ ] 누를 때마다 LED 반전

### 4주차 · UART
- [ ] PuTTY 설치
- [ ] PC 터미널에 문자 출력
- [ ] 버튼 누르면 "pressed" 출력

---

## 주문 부품 (9/8 도착 예정)

- MPU-6050 (GY-521) IMU — 핀헤더 납땜 완료품, 6축(가속도3+자이로3), 3.3V 연결
- 브레드보드 MB-102 830핀 ×2
- 듀폰 점퍼선 20cm 3종(암암/암수/수수)
- 아크릴 베이스판 20×12cm (10mm 육각 서포터 포함)
- 로직 애널라이저 8ch 24MHz (PulseView 무료 SW)
- 스텝모터 28BYJ-48 + ULN2003 드라이버 (5V, 감속비 1:64, 4096스텝/회전)

---

## 참고 자료

- CSAPP 3장 · 6장 · 8장 (도서관)
- Miro Samek, *Modern Embedded Systems Programming* (유튜브, 무료)
- 위키독스 "ARM 프로그래밍 실무" (한글, 무료)
- Godbolt (compiler explorer) — C가 어셈블리로 어떻게 되는지

---

# 일일 기록

> 형식: **뭘 했는지 / 뭐가 안 됐는지.**
> 안 된 쪽이 나중에 더 값나감.

## 2026-09-06 (일) 23:12 KST — 레지스터 직접 제어 (2주차 조기 완료)

**한 일**

HAL 함수를 걷어내고 레지스터로 LED 제어.

```c
/* USER CODE BEGIN 2 */
RCC->AHB1ENR |= (1 << 0);      // GPIOA 클럭 공급
GPIOA->MODER |= (1 << 10);     // PA5 출력 모드

/* USER CODE BEGIN 3 */
GPIOA->ODR ^= (1 << 5);
for (volatile int i = 0; i < 5000000; i++);
```

- RM0383 8.4.1 MODER 표에서 비트 위치를 직접 확인하고 코드와 대조
- `MX_GPIO_Init()` 내부를 열어보니 `__HAL_RCC_GPIOA_CLK_ENABLE()` → 결국 `SET_BIT(RCC->AHB1ENR, ...)`. **HAL도 같은 레지스터를 건드릴 뿐**임을 확인

**막혔던 것 / 알아낸 것**

1. **클럭 없으면 아무 일도 안 일어남** — `MX_GPIO_Init()`과 클럭 설정을 함께 주석 처리하니 LED 완전 무반응. 에러도 없음. 실험으로 확인함

2. **지연 루프가 동작 안 함** — 원인은 **지역 변수 미초기화**(`for (volatile int i; ...)`).
   스택 잔여값이 조건보다 커서 루프가 즉시 종료됨. C 표준상 undefined behavior.
   → 재현이 안 되는 유형의 버그. 빌드 상태에 따라 결과가 바뀜.
   → 전역/static은 0으로 자동 초기화되지만 지역 변수는 아님.
   → `-Wall` 켜두면 사전에 잡힘 (설정 예정)

3. **LED가 어둡게 켜지는 현상** — 고장이 아니라 **PWM을 우연히 재현한 것**.
   지연이 짧아 초당 수천 회 점멸 → 눈이 평균 밝기로 인식.
   켜짐/꺼짐 비율(듀티 사이클)을 바꾸면 밝기가 변함. 12월 모터 속도 제어와 같은 원리

**측정**

- 지연 루프 5,000,000회 ≈ 0.5초 (`HAL_Delay(500)`과 비슷)
- 역산: 초당 약 1,000만 회. 클럭 84MHz 기준 **1회당 약 8사이클**
- `volatile` 때문에 매 반복 메모리 read-compare-add-write가 발생하는 탓

**메모**

- 지연 루프는 클럭·최적화 레벨·컴파일러에 따라 시간이 달라져 실무용이 아님 → 정확한 타이밍은 **타이머** 필요 (10월)
- 최적화는 학습 중 `-O0` 유지 (`-O2`는 디버깅 시 브레이크포인트가 어긋남)
- CMSIS 헤더의 `__IO` = `volatile`. 레지스터에는 이미 붙어 있음

**다음**

- 깃 저장소 생성 후 첫 커밋
- 3주차: PC13 버튼 (GPIOC 클럭 별도)
- 9/8 부품 도착 시 IMU·모터 순으로

---

## 2026-09-05 (토) 14:13 KST — 첫 LED 점등 성공

**한 일**
- 보드 수령, 개발 환경 구축 완료
- `01-led-blink` 프로젝트 생성 → HAL로 PA5 토글, 500ms → 100ms 변경 확인

**막혔던 것**

1. **CubeIDE에 STM32 프로젝트 생성 메뉴가 없음** — 약 40분 소요
   - **원인: 2.0.0부터 CubeMX가 IDE에서 분리됨.** 웹 검색으로 확인
   - 해결: CubeMX 별도 설치 후 거기서 코드 생성

2. **빌드 에러 4건** — 전부 오타 (`GPIO`→`GPIOA`, `HAL_DELAY`→`HAL_Delay`)
   - C는 대소문자 구분. `HAL_` 입력 후 **Ctrl+Space** 자동완성으로 방지

3. CubeMX 첫 실행 시 "Error during Data Refresh" — 재시작으로 해결

**메모**
- Defender 예외 설정함 / 워크스페이스 경로에 한글 금지
- 코드는 반드시 `USER CODE BEGIN/END` 주석 사이에 — 밖에 쓰면 재생성 시 삭제됨
